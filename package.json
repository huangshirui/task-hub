{
  "name": "task-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:worker": "npm run build && node --test dist/tests/worker/*.test.js",
    "test:runner": "python runner/run_tests.py",
    "test": "npm run test:worker && npm run test:runner"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260609.0",
    "@types/node": "^25.9.3",
    "typescript": "^5.9.3"
  }
}
